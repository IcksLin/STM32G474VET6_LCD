# User-owned build entry point.
#
# GNU Make prefers this file over Makefile.  STM32CubeMX remains the sole owner
# of Makefile, while project-specific sources are maintained in project.mk.
include Makefile
include project.mk
